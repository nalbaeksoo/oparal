# AGENTS Instructions

This repository contains a Bash script (`oparal.sh`) and supporting documentation.

When modifying code or documentation you must run the following commands before committing:

```bash
bash -n oparal.sh
./oparal.sh -h | head -n 20
```

Use 2-space indentation in shell scripts. All new features should be documented in `README.md`.
